<?xml version="1.0" encoding="utf-8"?>
<!--
  toast_custom — layout de un Toast/snackbar personalizado con icono y mensaje.
  Usado para mostrar notificaciones breves con estilo propio en toda la app.
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/toastRoot"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingStart="16dp"
    android:paddingEnd="20dp"
    android:paddingTop="12dp"
    android:paddingBottom="12dp"
    android:background="@drawable/bg_toast"
    android:elevation="4dp">

    <!-- Icono del toast (info/éxito/error según el caso) -->
    <ImageView
        android:id="@+id/ivToastIcono"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:src="@drawable/ic_info"
        android:contentDescription="@null" />

    <!-- Mensaje de texto del toast -->
    <TextView
        android:id="@+id/tvToastMensaje"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="10dp"
        android:textSize="14sp"
        android:maxWidth="260dp"
        android:textColor="@color/white" />

</LinearLayout>