<?xml version="1.0" encoding="utf-8"?>
<!--
  dialog_custom — Diálogo genérico de confirmación/alerta con icono, título y mensaje.
  Reutilizable para mostrar mensajes de confirmación con botones de confirmar y cancelar.
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/dialogRoot"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp">

    <!-- Icono opcional del diálogo -->
    <ImageView
        android:id="@+id/ivDialogIcono"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_gravity="center_horizontal"
        android:src="@drawable/ic_info"
        android:contentDescription="@null"
        android:visibility="gone" />

    <!-- Título del diálogo -->
    <TextView
        android:id="@+id/tvDialogTitulo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="8dp"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textColor="?attr/colorOnSurface" />

    <!-- Mensaje descriptivo del diálogo -->
    <TextView
        android:id="@+id/tvDialogMensaje"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="12dp"
        android:textSize="14sp"
        android:textColor="?attr/colorOnSurface"
        android:alpha="0.7"
        android:gravity="center" />

    <!-- Botón de confirmación -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnDialogConfirmar"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:layout_marginTop="24dp"
        android:text="@string/dialog_confirmar"
        app:cornerRadius="12dp"
        xmlns:app="http://schemas.android.com/apk/res-auto" />

    <!-- Botón de cancelación -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnDialogCancelar"
        style="@style/Widget.Material3.Button.OutlinedButton"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:layout_marginTop="8dp"
        android:text="@string/dialog_cancelar"
        app:cornerRadius="12dp"
        xmlns:app="http://schemas.android.com/apk/res-auto" />

</LinearLayout>