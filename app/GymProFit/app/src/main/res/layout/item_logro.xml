<?xml version="1.0" encoding="utf-8"?>
<!--
  item_logro — item de RecyclerView para un logro/achievement del usuario.
  Muestra icono, nombre, descripción y marca de desbloqueado, usado en la pantalla de logros.
  El icono es un vector teñido por tema (ver LogroAdapter): el emoji anterior no
  se teñía, no se alineaba con el texto y, al venir de un campo que la API no
  envía, acababa siendo el MISMO trofeo en los seis logros.
-->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="10dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="0dp"
    app:cardBackgroundColor="?attr/colorSurface">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:padding="16dp">

        <!-- Icono del logro: vector teñido, uno por tipo -->
        <ImageView
            android:id="@+id/ivIconoLogro"
            android:layout_width="34dp"
            android:layout_height="34dp"
            android:layout_marginStart="5dp"
            android:layout_marginEnd="5dp"
            app:tint="?attr/colorPrimary"
            android:contentDescription="@null" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="12dp"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tvNombreLogro"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="@dimen/text_body"
                android:textStyle="bold"
                android:textColor="?attr/colorOnSurface" />

            <TextView
                android:id="@+id/tvDescLogro"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:textSize="@dimen/text_label"
                android:textColor="?attr/colorOnSurface"
                android:alpha="0.6" />

        </LinearLayout>

        <!-- Checkmark si desbloqueado -->
        <ImageView
            android:id="@+id/ivDesbloqueado"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/ic_check"
            android:visibility="gone"
            android:contentDescription="@null" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
